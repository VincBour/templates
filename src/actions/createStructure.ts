import { Uri } from "vscode";
import { FileSettingsType } from "../types";

export const createStructure = (name: string, structure: FileSettingsType[], targetUri: string | Uri | undefined) => {
    //* TODO  create workspaceEdit

    //* TODO manage create file or directory with right name

    //* TODO apply Edit in workspace

    //* TODO manage internaly file modification
};