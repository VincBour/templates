import { Uri } from "vscode";

export const getTargetUri = async (resource: Uri | string | undefined) => {
    //* TODO manage if resource is String

    //* TODO manage if resource is undefined

    return resource;
};