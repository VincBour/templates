import { PathLike } from "fs";
import { FolderType } from "../types";

export const getTemplates = (path: PathLike): FolderType[] => {
    //* TODO read directory

    //* TODO filter by isDirectory

    //* TODO get directory content

    //* TODO transform content to structure
    
    return [];
};