import * as React from 'react';
import { cleanup, render, screen } from '@testing-library/react';
import user from '@testing-library/user-event';
import { FCName } from '../[FCName]';

describe('[FCName]', () => {
    afterEach(() => {
        cleanup();
    });
    it('should ', () => {
        // Given

        // When
        render(<[FCName]></[FCName]>);
        //Then

    });
});